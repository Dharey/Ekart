<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 
          http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <pluginGroups>
        <pluginGroup>org.sonarsource.scanner.maven</pluginGroup>
    </pluginGroups>

    <servers>
        <server>
            <id>nexus-releases</id>
            <username>${env.NEXUS_USER}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>nexus-snapshots</id>
            <username>${env.NEXUS_USER}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
    </servers>

    <mirrors>
        <mirror>
            <id>nexus-maven-central</id>
            <mirrorOf>*</mirrorOf>
            <url>https://nexus.deetechpro.com/repository/maven-public/</url>
        </mirror>
    </mirrors>

    <profiles>
        <profile>
            <id>release</id>
            <repositories>
                <repository>
                    <id>nexus-releases</id>
                    <url>https://nexus.deetechpro.com/repository/maven-releases/</url>
                </repository>
            </repositories>
        </profile>

        <profile>
            <id>snapshot</id>
            <repositories>
                <repository>
                    <id>nexus-snapshots</id>
                    <url>https://nexus.deetechpro.com/repository/maven-snapshots/</url>
                </repository>
            </repositories>
        </profile>

        <profile>
            <id>sonar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <sonar.host.url>https://sonarqube.deetechpro.com/</sonar.host.url>
            </properties>
        </profile>
    </profiles>

</settings>